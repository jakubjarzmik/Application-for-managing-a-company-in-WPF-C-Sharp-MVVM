<local:WszystkieViewBase x:Class="Firma.Views.PojazdyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Firma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Path=PojazdId}"/>
                <DataGridTextColumn Header="Rodzaj Pojazdu" Binding="{Binding Path=RodzajPojazdu}"/>
                <DataGridTextColumn Header="Marka" Binding="{Binding Path=Marka}"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Path=Model}"/>
                <DataGridTextColumn Header="Rocznik" Binding="{Binding Path=Rocznik}"/>
                <DataGridTextColumn Header="VIN" Binding="{Binding Path=NrVIN}"/>
                <DataGridTextColumn Header="Kraj rej." Binding="{Binding Path=KrajRejestracji}"/>
                <DataGridTextColumn Header="Nr rej." Binding="{Binding Path=NrRejestracyjny}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
